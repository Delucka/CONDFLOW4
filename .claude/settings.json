{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "allow": [
      "Skill(graphify-windows)",
      "Skill(graphify-windows:*)"
    ]
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"hookSpecificOutput\":{\"hookEventName\":\"SessionStart\",\"additionalContext\":\"CondoFlow project rule: IF the graphify-windows skill is listed as available, invoke it via the Skill tool to refresh the knowledge graph before answering questions about the codebase or making code changes, then proceed with the user request normally. IF it is not listed, skip it silently and proceed with the user request — the skill lives outside the repository, so remote and non-Windows sessions do not have it. Its absence is expected there and is not an error worth reporting.\"}}'",
            "statusMessage": "Verificando o graphify-windows..."
          }
        ]
      }
    ]
  }
}
