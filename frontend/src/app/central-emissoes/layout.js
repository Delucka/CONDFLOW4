export const metadata = {
  title: 'Central de Emissões | CondoFlow',
};

export default function CentralEmissoesLayout({ children }) {
  return (
    <div className="animate-fade-in pb-12">
      {children}
    </div>
  );
}
